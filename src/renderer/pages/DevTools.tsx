import React from "react";
import TransferSimulator from "../components/development/TransferSimulator";

const DevToolsPage: React.FC = () => {
  return (
    <div>
      <h1>开发工具</h1>
      <TransferSimulator />
    </div>
  );
};

export default DevToolsPage;
